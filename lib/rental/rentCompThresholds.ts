/** Minimum parsed rent listings required to compute a weekly band. */
export const MIN_RENT_COMPS_FOR_BAND = 3;

/** Enough total comps to compute a band (discovery may continue for a richer picker pool). */
export const TARGET_RENT_COMPS_FOR_BAND = 5;

/** Prefer at least this many comps in the subject suburb before stopping discovery early. */
export const MIN_SAME_SUBURB_COMPS_FOR_DISCOVER = 3;
